// const webpack = require('webpack')
// const buildConfig = require('./webpack.build')
// const ExtractTextPlugin = require('extract-text-webpack-plugin')
//
// buildConfig.output.filename = 'x-vue.min.js'
//
// buildConfig.plugins.push(new ExtractTextPlugin('x-vue.min.css'))
// buildConfig.plugins.push(new webpack.optimize.UglifyJsPlugin({
//   compress: {
//     warnings: false
//   }
// }))
//
// module.exports = buildConfig

const path = require('path')
const ExtractTextPlugin = require('extract-text-webpack-plugin')
const webpack = require('webpack')

const baseConfig = require('./webpack.base')()

const config = Object.assign({}, baseConfig, {
  devtool: '',
  debug: false,
  entry: {
    app: path.join(__dirname, '../src/index.js')
  },
  output: {
    path: path.join(__dirname, '../dist'),
    filename: 'x-vue.min.js',
    library: 'XVue',
    libraryTarget: 'umd',
    umdNamedDefine: true
  },
  plugins: [
    new webpack.DefinePlugin({
      'process.env': {
        NODE_ENV: '"production"'
      }
    }),
    new webpack.optimize.UglifyJsPlugin({
      compress: {
        warnings: false
      }
    }),
    new ExtractTextPlugin('x-vue.min.css'),
    new webpack.optimize.OccurenceOrderPlugin()
  ]
})

config.module.loaders.push({
  test: /\.styl/,
  loader: ExtractTextPlugin.extract('style', 'css!postcss!stylus')
})
config.vue.loaders = {
  css: ExtractTextPlugin.extract('css'),
  stylus: ExtractTextPlugin.extract('css!stylus')
}

module.exports = config
